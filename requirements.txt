# ---------------------------------------------------------------------------
# GENERATED FALLBACK — not the source of truth.
#
# Dependencies are declared in pyproject.toml and pinned in uv.lock.
# This file exists only so a reviewer without uv installed can still run
# `pip install -r requirements.txt`.
#
# Regenerate it from the lockfile with:
#     uv export --no-dev --no-hashes --format requirements-txt > requirements.txt
#     (or: make export)
#
# Unlike uv.lock, this lists direct dependencies only — transitive
# versions are left to the resolver, so it is NOT reproducible.
# ---------------------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
prometheus-client==0.21.0
pika==1.3.2
